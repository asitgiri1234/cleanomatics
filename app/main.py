"""FastAPI application entry point.

Wires the app together. Routes are registered in `app.api` and are not
implemented yet.
"""

from fastapi import FastAPI

app = FastAPI(title="Cleanomatics Support Assistant")
